# === Core Framework ===
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
python-multipart==0.0.9
python-dotenv==1.0.1

# === LLM Clients ===
# openai: OpenAI-compatible SDK shared by Groq / Cerebras / xAI / OpenRouter.
openai==1.51.0
# google-genai: native Gemini SDK — Gemini is part of the provider fallback chain.
google-genai==1.74.0
# httpx: used directly by scripts/contract_selftest.py (also an openai dependency).
httpx>=0.27.0

# === Embeddings & Vector Store ===
sentence-transformers==2.7.0
transformers==4.44.0
faiss-cpu==1.8.0
torch>=2.1.0

# === Retrieval ===
langchain-community>=0.3.25
langchain-core>=0.3.65
langchain-text-splitters>=0.3.8
rank-bm25==0.2.2

# === Document Processing ===
# OS dep: install poppler-utils for cleaner PDF text extraction (apt: poppler-utils, brew: poppler).
# The pipeline falls back to PyPDF2 automatically if pdftotext isn't on PATH.
PyPDF2==3.0.1
docx2txt==0.8

# === Arabic NLP ===
# farasapy is optional and currently unused

# === Evaluation UI ===
streamlit==1.38.0

# === Data & Viz ===
numpy==1.26.4
pandas==2.2.2
matplotlib==3.9.2
scikit-learn==1.5.1

# === Utilities ===
tqdm==4.66.5
